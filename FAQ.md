# ❓ Часто задаваемые вопросы (FAQ)

## Общие вопросы

### Что такое GdeDoctor Bot?

GdeDoctor Bot - это Telegram бот для поиска врачей в медицинских учреждениях Калуги. Бот помогает найти нужного специалиста, показывает местоположение на карте и позволяет читать и оставлять отзывы.

### Бесплатно ли использование бота?

Да, бот полностью бесплатный для пользователей.

### Откуда берутся данные о врачах?

Данные хранятся в локальной SQLite базе данных (`medical_data.db`). База содержит информацию о врачах, больницах и специальностях в Калуге.

## Установка и настройка

### Какая версия Python нужна?

Python 3.9 или выше.

### Где получить Telegram Bot Token?

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

### Где получить Yandex API Key?

1. Зарегистрируйтесь на [Yandex.Cloud](https://cloud.yandex.ru/)
2. Создайте API ключ для Geocoder API
3. Скопируйте ключ

### Нужен ли API ключ для Static Maps?

Нет! Static Maps API v1 работает без API ключа. Ключ нужен только для Geocoder API (преобразование адресов в координаты).

## Использование

### Как найти врача?

1. Отправьте `/start`
2. Нажмите "🔍 Найти врача"
3. Выберите специальность
4. Выберите медицинское учреждение
5. Выберите врача из списка

### Как оставить отзыв?

1. Найдите врача
2. Нажмите "✍️ Оставить отзыв"
3. Напишите текст отзыва (10-2000 символов)
4. Отправьте сообщение

### Можно ли редактировать отзыв?

Нет, редактирование отзывов пока не поддерживается.

### Как вернуться назад?

На каждом этапе есть кнопка "◀️ Назад". Также можно использовать команду `/cancel`.

## Проблемы и решения

### Бот не отвечает

**Возможные причины:**
- Бот не запущен
- Неправильный токен в `.env`
- Проблемы с интернетом

**Решение:**
1. Проверьте, запущен ли бот
2. Проверьте токен в `.env`
3. Перезапустите бота

### Ошибка "Token is invalid"

**Решение:**
- Проверьте TELEGRAM_TOKEN в `.env`
- Убедитесь, что токен скопирован полностью без пробелов
- Получите новый токен у @BotFather

### Карты не отображаются

**Причины:**
- Неправильный Yandex API ключ
- Нет интернета
- Адрес не найден

**Решение:**
- Проверьте YANDEX_API_KEY в `.env`
- Убедитесь, что ключ активен
- Проверьте интернет-соединение

**Примечание:** Даже если карты не загружаются, ссылки на Яндекс.Карты всё равно работают!

### Ошибка "Module not found"

**Решение:**
```bash
cd bot
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/macOS
pip install -r requirements.txt
```

### База данных не найдена

**Решение:**
- Убедитесь, что файл `medical_data.db` находится в корне проекта
- Проверьте путь в `.env`: `DATABASE_PATH=medical_data.db`

### Бот зависает при поиске

**Решение:**
1. Остановите бота (`Ctrl+C`)
2. Проверьте логи на ошибки
3. Перезапустите бота

## Разработка

### Как добавить новых врачей в базу?

База данных SQLite находится в файле `medical_data.db`. Вы можете редактировать её с помощью любого SQLite клиента (например, DB Browser for SQLite).

### Можно ли изменить тексты сообщений?

Да, все тексты находятся в файле `bot/app/constants.py` в классах `Messages` и `ButtonLabels`.

### Как добавить новую команду?

1. Создайте обработчик в `bot/app/handlers/`
2. Зарегистрируйте роутер в `bot/app/main.py`
3. Добавьте текст команды в `bot/app/constants.py`

### Как изменить лимиты отзывов?

Откройте `bot/app/constants.py` и измените значения в классе `Limits`:
```python
class Limits:
    REVIEW_MIN_LENGTH = 10    # Минимум символов
    REVIEW_MAX_LENGTH = 2000  # Максимум символов
```

## Технические вопросы

### Какие зависимости использует проект?

- aiogram 3.4+ - фреймворк для Telegram ботов
- aiohttp - асинхронные HTTP запросы
- aiosqlite - работа с SQLite
- python-dotenv - переменные окружения

### Поддерживается ли PostgreSQL?

Нет, проект использует только SQLite. Но вы можете адаптировать код для PostgreSQL.

### Можно ли запустить несколько ботов?

Да, но каждому нужен свой токен и своя база данных.

### Как обновить бота?

```bash
git pull origin main
cd bot
pip install -r requirements.txt --upgrade
```

## Безопасность

### Безопасно ли хранить токены в .env?

Да, если файл `.env` добавлен в `.gitignore` и не попадает в git. **Никогда не публикуйте токены в открытом доступе!**

### Как защитить базу данных?

- Регулярно делайте резервные копии
- Не давайте прямой доступ к файлу БД
- Используйте права доступа файловой системы

## Производительность

### Сколько пользователей может обслужить бот?

Зависит от сервера. SQLite хорошо работает для небольших и средних нагрузок (до нескольких тысяч пользователей одновременно).

### Как ускорить работу бота?

- Используйте SSD для базы данных
- Оптимизируйте SQL запросы
- Добавьте индексы в БД
- Используйте кэширование

## Поддержка

### Где получить помощь?

1. Прочитайте [README.md](README.md)
2. Проверьте этот FAQ
3. Посмотрите [Issues на GitHub](https://github.com/Syricoff/GdeDoctorBot/issues)
4. Создайте новый Issue с описанием проблемы

### Как сообщить об ошибке?

Создайте Issue на GitHub с:
- Описанием проблемы
- Шагами для воспроизведения
- Логами (если есть)
- Версией Python и ОС

---

**Не нашли ответ?** Создайте [Issue на GitHub](https://github.com/Syricoff/GdeDoctorBot/issues)!
